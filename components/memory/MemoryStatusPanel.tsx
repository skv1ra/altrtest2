import { Activity, BrainCircuit, CalendarClock, Pause, Play } from "lucide-react";

export function MemoryStatusPanel({ learning, total, active, lastUpdate, onToggle }: { learning: boolean; total: number; active: number; lastUpdate: string; onToggle: () => void }) {
  return <section className="memory-status-panel"><div className="memory-status-main"><div><p className="data-label">LEARNING STATUS</p><div className="mt-3 flex items-center gap-3"><span className={`h-2 w-2 rounded-full ${learning?"bg-cyan-100 shadow-[0_0_14px_rgba(103,232,249,.85)]":"bg-amber-200/60"}`} /><h2 className="text-2xl font-medium">{learning ? "Active" : "Paused"}</h2></div><p className="mt-3 max-w-md text-xs leading-5 text-white/32">{learning ? "Learning resumed. Altr can continue improving your digital self." : "Learning is paused. Altr will not create new memories, but existing memory can still be used."}</p></div><button onClick={onToggle} className="glass-button inline-flex items-center gap-2 rounded-full px-4 py-2.5 text-xs">{learning?<Pause className="h-3.5 w-3.5" />:<Play className="h-3.5 w-3.5" />}{learning?"Pause Learning":"Resume Learning"}</button></div><div className="memory-stat"><BrainCircuit /><strong>{total}</strong><span>Total memories</span></div><div className="memory-stat"><Activity /><strong>{active}</strong><span>Active memories</span></div><div className="memory-stat"><CalendarClock /><strong className="text-base">{lastUpdate}</strong><span>Last update</span></div></section>;
}
